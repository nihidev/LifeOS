[tool.black]
line-length = 88
target-version = ["py312"]

[tool.ruff]
select = ["E", "F", "W"]
line-length = 88

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
concurrency = ["greenlet"]
